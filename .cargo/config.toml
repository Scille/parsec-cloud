[target.wasm32-unknown-unknown]
rustflags = [
    # getrandom@0.3 require to specify the random backend to wasm_js for it to work with Crypto.getRandomValues API
    # https://docs.rs/getrandom/0.3.3/getrandom/#webassembly-support
    "--cfg",
    'getrandom_backend="wasm_js"',
]
