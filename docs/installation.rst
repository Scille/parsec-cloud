.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install parsec

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv parsec
    $ pip install parsec
