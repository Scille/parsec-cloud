RUSTFS_TLS_PATH=/tls
RUSTFS_ADDRESS=parsec-s3:9000
# MUST be similar to RUSTFS_ADDRESS with the colon escaped,
# Not used directly by `rustfs` but will be useful for build the blockstore URL for parsec
RUSTFS_ESCAPED_ADDRESS=parsec-s3\:9000
RUSTFS_REGION=region1
BUCKET_NAME=parsec-testing-infra
