{
    "name": "libparsec_bindings_web",
    "private": true,
    "version": "3.5.0-a.6.dev.20315+a1f1b2b",
    "license": "BUSL-1.1",
    "scripts": {
        "build": "node scripts/build.js",
        "build:dev": "npm run build -- dev",
        "build:ci": "npm run build -- ci",
        "build:release": "npm run build -- release",
        "test": "wasm-pack test"
    }
}
