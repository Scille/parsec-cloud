 [CLI][Breaking] Harmonize ``--force`` option.

    Impacted commands:
    - forget local device
    - tos accept (replace ``--yes`` by ``--force``)
    - device overwrite server_url
    - shared-recovery create (replace ``--no-confirmation`` by ``--force``)
