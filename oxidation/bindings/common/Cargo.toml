[package]
name = "libparsec_bindings_common"
version = "0.1.0"
edition = "2021"

[dependencies]
libparsec = { path = "../../libparsec", default_features = false, features = ["use-rustcrypto"] }
base64 = "0.20.0"
serde_json = "1.0.85"
