[package]
name = "libparsec_platform_native"
version = "0.0.0"
edition = "2021"

[[test]]
name = "integration"
path = "tests/tests.rs"

[dependencies]
http = "0.2.6"
futures = "0.3.21"
dotenv = "0.15.0"
smol = "1.2.5"
async-std = "1.10.0"
async-tungstenite = { version = "0.17.1", features = ["async-std-runtime"] }

[dev-dependencies]
parsec_api_protocol = { version = "0.0.0", path = "../parsec_api_protocol" }

rstest = "0.12.0"
