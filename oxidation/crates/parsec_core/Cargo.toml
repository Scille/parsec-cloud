[package]
name = "parsec_core"
version = "0.0.0"
edition = "2021"
license = " BUSL-1.1"
autotests = false

[[test]]
name = "integration"
path = "tests/mod.rs"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
api_crypto = { version = "0.0.0", path = "../../libparsec/crates/api_crypto" }
parsec_api_protocol = { version = "0.0.0", path = "../parsec_api_protocol" }
parsec_api_types = { version = "0.0.0", path = "../parsec_api_types" }

thiserror = "1.0.30"

[dev-dependencies]
tests_fixtures = { version = "0.0.0", path = "../tests_fixtures" }

rstest = "0.13.0"
chrono = "0.4.19"
